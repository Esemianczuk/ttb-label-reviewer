{
  "objective": "Improve public COLA label evidence extraction while keeping deterministic validation as the final authority.",
  "splitting_policy": {
    "unit": "record/application",
    "why": "Images from the same COLA record must not appear in both train and validation/test.",
    "default": {
      "train": 0.7,
      "val": 0.15,
      "test": 0.15
    }
  },
  "models": [
    {
      "name": "PaddleOCR PP-OCR",
      "role": "primary backend detector/recognizer candidate",
      "train": ["text_detection", "text_recognition", "angle_classifier"],
      "use_when": "We have reviewed quadrilateral text boxes plus line transcripts.",
      "expected_value": "Best long-term path for arbitrary orientation, dense label layouts, and GPU/CPU production deployment.",
      "risk": "Requires the most annotation discipline and package/runtime management."
    },
    {
      "name": "EasyOCR",
      "role": "backend escalation and fast curation detector",
      "train": ["custom_recognition_network"],
      "use_when": "Default detector is good enough but recognition fails on stylized label text.",
      "expected_value": "Already works well locally and can be plugged into the worker path.",
      "risk": "Custom recognition training is less turnkey than PaddleOCR and does not solve layout by itself."
    },
    {
      "name": "Tesseract LSTM",
      "role": "browser/CPU baseline and specialty recognizer candidate",
      "train": ["line_recognition"],
      "use_when": "We have clean line crops and want small CPU-side improvements for a narrow alphabet/style.",
      "expected_value": "Small deploy footprint and browser-adjacent baseline.",
      "risk": "Training is ground-truth-file heavy and not a field/crop detector."
    },
    {
      "name": "CLIP or SigLIP region ranker",
      "role": "field-region reranker, not exact OCR",
      "train": ["field_crop_classification", "candidate_ranking"],
      "use_when": "We need to choose which detected text blob is likely ABV, warning, brand, producer, etc.",
      "expected_value": "Can learn label-layout semantics and reduce OCR passes.",
      "risk": "Not safe for exact text or numeric validation; must feed OCR/validators."
    },
    {
      "name": "docTR",
      "role": "alternative OCR pipeline benchmark",
      "train": ["detector", "recognizer"],
      "use_when": "PaddleOCR integration cost is high or we want a PyTorch-native OCR benchmark.",
      "expected_value": "Good research/benchmark option with deep-learning OCR pieces.",
      "risk": "Less aligned with current worker scheduler than EasyOCR/PaddleOCR."
    }
  ],
  "acceptance_metrics": [
    "field_recall_by_required_field",
    "exact_or_normalized_text_match_rate",
    "evidence_crop_iou_or_reviewer_acceptance",
    "ocr_ms_p50_p95",
    "reviewer_override_rate",
    "false_pass_rate_on_intentional_mismatches"
  ],
  "promotion_rule": "Only promote a trained model when it improves validation-set field recall without increasing false passes on the untouched test set."
}
