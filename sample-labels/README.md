# Sample Labels

The runnable sample images live in `public/sample-labels/`:

- `old-tom-front.png`
- `old-tom-back.png`

Use **Load Sample Packet** in the app to load both images with matching expected fields. These synthetic images are reviewed as one label packet because the front and back contain different required evidence.
